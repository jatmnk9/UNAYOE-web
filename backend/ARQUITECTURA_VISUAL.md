# 🏗️ Arquitectura Visual del Backend UNAYOE

## 📊 Diagrama de Capas (MVC)

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENTE HTTP                              │
│                    (Frontend React/Vue)                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CAPA DE PRESENTACIÓN                          │
│                    (Routers/Controllers)                         │
├─────────────────────────────────────────────────────────────────┤
│  auth.py         │  users.py       │  notes.py                  │
│  - POST /login   │  - POST /...    │  - GET /notas/{id}         │
│                  │  - GET /...     │  - POST /notas             │
├──────────────────┼─────────────────┼────────────────────────────┤
│  analysis.py     │  recommendations│  appointments.py           │
│  - GET /analyze  │  - GET /rec...  │  - POST /citas             │
│  - GET /export   │  - POST /likes  │  - GET /citas/...          │
└───────────────────────────┬─────────────────────────────────────┘
                            │ Dependency Injection
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CAPA DE LÓGICA DE NEGOCIO                     │
│                         (Services)                               │
├─────────────────────────────────────────────────────────────────┤
│  AuthService           │  UsersService       │  NotesService    │
│  - login()             │  - crear_est...     │  - obtener_...   │
│                        │  - crear_psi...     │  - guardar_...   │
├────────────────────────┼─────────────────────┼──────────────────┤
│  AnalysisService       │  RecommendationsS.. │  AppointmentsS.. │
│  - analizar_...        │  - obtener_...      │  - crear_cita()  │
│  - crear_vis...        │  - agregar_like()   │  - asignar_...   │
├────────────────────────┼─────────────────────┼──────────────────┤
│  NLPService (Singleton)         │  AlertService                 │
│  - preprocesar_texto()          │  - trigger_alert_...          │
│  - analizar_sentimiento()       │  - send_email()               │
│  - analizar_emocion()           │  - compute_risk()             │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CAPA DE ACCESO A DATOS                        │
│                    (Database Client)                             │
├─────────────────────────────────────────────────────────────────┤
│              Supabase Client (Singleton)                         │
│              - get_supabase_client()                             │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BASE DE DATOS                                 │
│                    PostgreSQL (Supabase)                         │
├─────────────────────────────────────────────────────────────────┤
│  usuarios  │  notas  │  recomendaciones  │  citas  │  likes     │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Flujo de una Petición HTTP

### Ejemplo: Guardar una Nota

```
1. Cliente
   │
   ├─> POST /notas
   │   Body: { "note": "Me siento triste", "user_id": "123" }
   │
   ▼
2. Router (notes.py)
   │
   ├─> Validación con Pydantic (Note schema)
   │   ✅ note: str ✅ user_id: str
   │
   ├─> Dependency Injection
   │   - NotesService
   │   - AlertService
   │
   ▼
3. NotesService
   │
   ├─> Inyecta NLPService
   │   └─> preprocesar_texto()
   │   └─> analizar_sentimiento()  → "NEG"
   │   └─> analizar_emocion()      → ("tristeza", 0.95)
   │
   ├─> Inyecta Supabase Client
   │   └─> INSERT INTO notas (usuario_id, nota, sentimiento, emocion, ...)
   │
   ▼
4. Background Task (AlertService)
   │
   ├─> contains_severe_keywords()
   │   └─> Detecta "triste" → No es palabra severa
   │   └─> No envía alerta
   │
   ▼
5. Response
   │
   └─> 200 OK
       {
         "message": "Nota guardada con éxito",
         "data": { ... }
       }
```

## 🔌 Patrones de Diseño

### 1. Singleton Pattern

```
┌──────────────────────────┐
│   Settings (Config)      │ ◄─── @lru_cache()
│   - Una sola instancia   │      (Garantiza Singleton)
└──────────────────────────┘

┌──────────────────────────┐
│   Supabase Client        │ ◄─── @lru_cache()
│   - Conexión única       │      get_supabase_client()
└──────────────────────────┘

┌──────────────────────────┐
│   NLPService             │ ◄─── Patrón __new__
│   - Modelos cargados 1x  │      + @lru_cache()
└──────────────────────────┘
```

### 2. Factory Pattern

```
┌──────────────────────────┐
│  get_auth_service()      │ ──► AuthService()
└──────────────────────────┘

┌──────────────────────────┐
│  get_users_service()     │ ──► UsersService()
└──────────────────────────┘

┌──────────────────────────┐
│  get_notes_service()     │ ──► NotesService()
└──────────────────────────┘

        ... etc ...
```

### 3. Dependency Injection

```
@router.post("/notas")
async def guardar_nota(
    note_data: Note,                          ◄─── Pydantic Model
    notes_service: NotesService = Depends(...) ◄─── Inyección de Servicio
):
    return notes_service.guardar_nota(...)
```

### 4. Repository Pattern

```
┌─────────────────────────────────────┐
│      NotesService                   │
├─────────────────────────────────────┤
│  - supabase: Client                 │ ◄─── Encapsula acceso a datos
│                                     │
│  + obtener_notas_por_usuario()      │
│  + guardar_nota()                   │
└─────────────────────────────────────┘
```

## 📦 Estructura de Módulos

```
backend/
│
├── main.py ──────────────────┐
│                             │
│                             ├─── Include Routers
│                             │
├── app/                      │
│   ├── config/               │
│   │   └── settings.py ◄─────┼─── Singleton
│   │                         │
│   ├── db/                   │
│   │   └── supabase.py ◄─────┼─── Singleton
│   │                         │
│   ├── models/               │
│   │   └── schemas.py ◄──────┼─── Pydantic Models
│   │                         │
│   ├── routers/ ◄────────────┘
│   │   ├── auth.py
│   │   ├── users.py
│   │   ├── notes.py
│   │   ├── analysis.py
│   │   ├── recommendations.py
│   │   └── appointments.py
│   │          │
│   │          ├─ Depends() ──┐
│   │          │               │
│   └── services/ ◄────────────┘
│       ├── nlp_service.py     (Singleton)
│       ├── auth_service.py    (Factory)
│       ├── users_service.py   (Factory)
│       ├── notes_service.py   (Factory)
│       ├── analysis_service.py (Factory)
│       ├── recommendations_service.py (Factory)
│       ├── appointments_service.py (Factory)
│       └── alert_service.py   (Factory)
```

## 🔐 Flujo de Seguridad

```
┌─────────────────────────────────────────────┐
│  .env (Variables de Entorno)                │
│  - SUPABASE_KEY (Service Role Key)          │
│  - GMAIL_SMTP_PASSWORD                      │
│  - GEMINI_API_KEY                           │
└────────────┬────────────────────────────────┘
             │
             ├─── Leído SOLO al inicio
             │
             ▼
┌─────────────────────────────────────────────┐
│  Settings (Singleton)                       │
│  - Valida con Pydantic                      │
│  - Convierte strings a tipos correctos      │
└────────────┬────────────────────────────────┘
             │
             ├─── Inyectado en servicios
             │
             ▼
┌─────────────────────────────────────────────┐
│  Services                                   │
│  - Usan settings.supabase_key               │
│  - NUNCA exponen en responses               │
└─────────────────────────────────────────────┘

    .env ──► .gitignore ✅ (NUNCA SE COMMITEA)
```

## 📊 Flujo de Datos NLP

```
Texto del Usuario
    │
    ▼
┌────────────────────────────────┐
│  NLPService.preprocesar_texto()│
│  - Lower case                  │
│  - Eliminar URLs               │
│  - Eliminar puntuación         │
│  - Tokenización                │
│  - Eliminar stopwords          │
└────────────┬───────────────────┘
             │
             ├─────────┬──────────────┐
             │         │              │
             ▼         ▼              ▼
    ┌───────────────────────┐  ┌──────────────────┐
    │ Sentiment Classifier  │  │ Emotion Classifier│
    │ (RoBERTa ES)          │  │ (BETO)            │
    │ ↓                     │  │ ↓                 │
    │ POS/NEG/NEU           │  │ joy/sadness/...   │
    └───────────────────────┘  └──────────────────┘
             │                          │
             └──────────┬───────────────┘
                        ▼
            ┌─────────────────────────┐
            │  Supabase (PostgreSQL)  │
            │  - sentimiento          │
            │  - emocion              │
            │  - emocion_score        │
            │  - tokens               │
            └─────────────────────────┘
```

## 🎯 Separación de Responsabilidades

### ❌ ANTES (Todo mezclado en backend.py)

```
┌──────────────────────────────────────────┐
│           backend.py (1081 líneas)       │
├──────────────────────────────────────────┤
│  • Configuración hardcodeada             │
│  • Imports mezclados                     │
│  • Funciones de NLP                      │
│  • Endpoints de FastAPI                  │
│  • Lógica de negocio                     │
│  • Acceso directo a Supabase             │
│  • Generación de gráficos                │
│  • Envío de emails                       │
│  • Sistema de alertas                    │
│  • Recomendaciones                       │
│  • Todo en un solo lugar! 😱             │
└──────────────────────────────────────────┘
```

### ✅ DESPUÉS (Organizado en capas)

```
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   Config Layer  │  │   Router Layer  │  │  Service Layer  │
├─────────────────┤  ├─────────────────┤  ├─────────────────┤
│  settings.py    │  │  auth.py        │  │  auth_service   │
│  - Variables    │  │  users.py       │  │  users_service  │
│  - Validación   │  │  notes.py       │  │  notes_service  │
└─────────────────┘  │  analysis.py    │  │  nlp_service    │
                     │  ...            │  │  ...            │
┌─────────────────┐  └─────────────────┘  └─────────────────┘
│   DB Layer      │           │                     │
├─────────────────┤           │                     │
│  supabase.py    │ ◄─────────┴─────────────────────┘
│  - Client       │           Dependency Injection
└─────────────────┘
```

## 🚀 Beneficios de la Arquitectura

| Aspecto | Antes | Después |
|---------|-------|---------|
| **Cohesión** | ❌ Baja (todo mezclado) | ✅ Alta (responsabilidades claras) |
| **Acoplamiento** | ❌ Alto (dependencias directas) | ✅ Bajo (DI, interfaces) |
| **Testabilidad** | ❌ Difícil (código monolítico) | ✅ Fácil (servicios aislados) |
| **Escalabilidad** | ❌ Limitada | ✅ Alta (fácil agregar features) |
| **Mantenibilidad** | ❌ Compleja | ✅ Simple (cambios localizados) |
| **Legibilidad** | ❌ Confusa | ✅ Clara (estructura intuitiva) |

---

**Versión:** 2.0.0
**Fecha:** Diciembre 2025
